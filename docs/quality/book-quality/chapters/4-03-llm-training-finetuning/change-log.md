# Change Log — Ch 03 (reading index 4): Training and Fine-Tuning

**2026-06-20 — Audit only — no edits applied.**

No `chapter.tex`, `.bib`, notebook, or figure file was modified by this audit.
This unit produced only the five report files in
`docs/quality/book-quality/chapters/4-03-llm-training-finetuning/`:
`constructive-review.md`, `skeptical-review.md`, `editor-plan.md`, `score.json`,
`change-log.md`.

Findings are recorded for a later `/iterate-book-quality` editing pass. Key
deferred actions (see `editor-plan.md`):
- MUST_FIX: rename duplicate labels `def:mlm` (vs ch01:1786) and
  `def:lora`/`eq:lora` (vs ch02:2184); reconcile the mismatched LoRA formula.
- SHOULD_FIX: replace/remove the `demo.ipynb` placeholder; flag or cache the
  live-EDGAR exercise cells.
