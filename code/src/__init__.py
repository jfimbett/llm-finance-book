"""
Placeholder package. The companion code for each chapter is self-contained in
``code/notebooks/<chapter>/`` (the ``exercises.ipynb`` / ``demo.ipynb`` notebooks and
the deterministic ``gen_*.py`` figure generators). There is no shared library to import;
this module exists only to keep ``code/`` importable as a namespace.
"""
