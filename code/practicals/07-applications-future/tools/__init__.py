"""Deterministic tools for the finance research-brief agent.

The agent (Claude Code / Cline) decides *which* tool to call and *interprets*
the results; these modules do every bit of the actual routing, retrieval, and
figure lookup, so the language model never computes or recalls a number itself.
"""
