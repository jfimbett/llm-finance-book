# REGRESSION_REPORT.md — Final Book-Quality Regression

> Generated by `/book-quality-regression`, 2026-06-20, after the release-quality pass.
> This is the final gate. Every check below was re-run fresh this turn (no trusting stale
> reports).

```
=== Book Quality Regression ===
Compiles:            YES  (pdflatex x3 + biber; 626 pages; -shell-escape for minted)
Duplicate bib keys:  0
Biber missing keys:  0
Print-leaking notes: 0    (no "Needs verification" / [CHECK] / [CITE: verify] in .bib)
Broken refs:         0
Unresolved cites:    0
Duplicate labels:    0    (chapters + appendices)
Hard-coded "Chapter N" prose: 0   (all converted to \Cref)
Use-before-def:      0    (per per-chapter concept-ordering re-audits)
Wrong-paper cites:   0    (C1–C5 corrected)
Orphaned finance ex: 0    (exercises/valuation_example wired into ch05)
Chapters passing:    16/16
Book-level pass:     YES
```

## Gate detail

| Gate | Result | Evidence |
|------|--------|----------|
| Build | ✅ YES | clean `pdflatex`×3 + `biber`; `main.pdf` 626 pp, ~2.64 MB |
| Bibliography hygiene | ✅ | 0 duplicate keys; 0 biber-missing; stub `shah2022flue` completed; `wei2022emergent` dedup; `chen2025aml` flagged `NEEDS_EXTERNAL_VERIFICATION` + uncited; all inline `% [CHECK]`/`% [CITE]` comments stripped |
| Cross-references | ✅ | 0 undefined `\ref`/`\eqref`/`\cite`; 0 duplicate labels; created `fig:ch11-rag-pipeline`; fixed `ch:responsible-llms`, `ch:llm-training-finetuning`, `app:huggingface` |
| Hard-coded chapter refs | ✅ | 0 `Chapter~N`/`Chapter~\ref` prose remain (all `\Cref`); both stale book-map tables rebuilt |
| Concept ordering | ✅ | per-chapter re-audits show no use-before-definition; ch12/ch13 forward-deps fixed |
| Repetition (SSOT) | ✅ | all cross-chapter label collisions collapsed to one source + `\Cref`; intra-chapter restatements (ch01, ch07, ch09, ch15) trimmed to cross-refs |
| Code / figures | ✅ | 11 committed deterministic `gen_*.py` generators; all referenced figures resolve and match prose; `run_illustrations.sh` covers them |
| Per-chapter scores | ✅ 16/16 | every chapter independently re-audited at ≥90 on all applicable dimensions (see `BOOK_SCORE.md`) |

## Book-level pass condition

All clauses satisfied:
1. every chapter passes — ✅ 16/16;
2. every book-level dimension ≥90 — ✅;
3. book compiles — ✅;
4. no broken `\ref`/`\eqref` — ✅;
5. no unresolved `\cite` — ✅;
6. no duplicate labels — ✅;
7. no stale critical reports — ✅ (`BOOK_SCORE`, `GOAL_STATUS`, this report all regenerated).

**BOOK-LEVEL PASS: YES.**

## Residual author follow-ups (non-blocking)

- Verify or delete `chen2025aml` (unverifiable future-dated arXiv id; currently uncited +
  flagged). The ch11 Adverse Media Index is presented as the book's own construct.
- Optionally restore precise working-paper numerics (currently hedged to qualitative) once
  verified against sources — `citation_accuracy` would only rise.
- Optionally delete unused `bibliography_bibertool.bib` / `bibliography_test.bib` (not
  loaded by `preamble.tex`).
- Optionally commit a pruned-vector snapshot for `gen_king_analogy.py` so it is offline-
  deterministic like the other ten generators (it currently regenerates from the pinned
  `glove-wiki-gigaword-300` release).
