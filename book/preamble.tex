% ============================================================
% Preamble — Large Language Models in Finance
% Typography modelled on Princeton UP academic monograph style
% (cf. Cochrane, The Fiscal Theory of the Price Level, 2023)
% ============================================================

% --- Font & Encoding ---
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{mathpazo}               % Palatino for text; Pazo Math for maths
\linespread{1.05}                   % Palatino needs slightly more leading
\usepackage[english]{babel}
\usepackage{microtype}              % Protrusion, expansion, kerning

% --- Page Layout ---
\usepackage[
  top         = 1.15in,
  bottom      = 1.15in,
  inner       = 1.35in,
  outer       = 1.10in,
  headheight  = 14pt
]{geometry}

% --- Math ---
\usepackage{amsmath, amssymb, amsthm, mathtools}
\DeclareMathOperator*{\argmax}{arg\,max}
\DeclareMathOperator*{\argmin}{arg\,min}

% --- Running Headers (fancyhdr) ---
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0.35pt}
\fancyhead[LE]{\small\itshape\nouppercase{\leftmark}}
\fancyhead[RO]{\small\itshape\nouppercase{\rightmark}}
\fancyfoot[C]{\small\thepage}
% Plain style for chapter-opening pages
\fancypagestyle{plain}{%
  \fancyhf{}
  \fancyfoot[C]{\small\thepage}
  \renewcommand{\headrulewidth}{0pt}
}

% --- Chapter & Section Headings (titlesec) ---
\usepackage{titlesec}

% Chapter: small-caps label + number, large bold title, framed by thin rules
\titleformat{\chapter}[display]
  {\normalfont\small\scshape\centering}
  {\chaptertitlename~\thechapter}
  {6pt}
  {\centering\Huge\bfseries\rmfamily}
  [\vspace{4pt}{\titlerule[0.5pt]}]

\titlespacing*{\chapter}{0pt}{-10pt}{36pt}

% Section: bold, numbered, with small space above
\titleformat{\section}
  {\normalfont\Large\bfseries}
  {\thesection}{1em}{}
\titlespacing*{\section}{0pt}{18pt plus 3pt minus 2pt}{8pt}

% Subsection: bold, slightly smaller
\titleformat{\subsection}
  {\normalfont\large\bfseries}
  {\thesubsection}{1em}{}
\titlespacing*{\subsection}{0pt}{14pt plus 2pt minus 2pt}{5pt}

% Subsubsection: italic bold, no number
\titleformat{\subsubsection}
  {\normalfont\normalsize\bfseries\itshape}
  {}{0em}{}
\titlespacing*{\subsubsection}{0pt}{10pt}{3pt}

% --- Colors ---
\usepackage{xcolor}
\definecolor{linkblue}{RGB}{14, 75, 140}
\definecolor{codebg}{RGB}{248, 248, 248}
\definecolor{coderule}{RGB}{210, 210, 210}
\definecolor{keywordblue}{RGB}{0, 100, 180}
\definecolor{commentgray}{RGB}{120, 120, 120}
\definecolor{stringgreen}{RGB}{30, 130, 30}
\definecolor{deepdivebg}{RGB}{235, 244, 255}
\definecolor{narrativebg}{RGB}{255, 252, 240}
\definecolor{narrativerule}{RGB}{180, 130, 40}
% Named colors used in figures (xcolor dvipsnames not loaded by default)
\definecolor{steelblue}{RGB}{70, 130, 180}
\definecolor{forestgreen}{RGB}{34, 139, 34}
\definecolor{crimson}{RGB}{220, 20, 60}
\definecolor{darkorange}{RGB}{255, 140, 0}

% --- Figures & Tables ---
\usepackage{graphicx}
\usepackage{tikz}
\usetikzlibrary{positioning,calc,decorations.pathreplacing}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usepackage{booktabs}
\usepackage{float}
\usepackage{caption}
\captionsetup{
  font       = small,
  labelfont  = bf,
  labelsep   = period,
  skip       = 8pt,
  width      = 0.92\textwidth,
}
\captionsetup[table]{position=above}

% --- Code Listings (minted + Pygments "friendly" theme) ---
\usepackage{minted}
\setminted{
  style      = friendly,
  fontsize   = \footnotesize,
  linenos    = true,
  frame      = lines,
  framesep   = 3mm,
  breaklines = true,
}

% --- Hyperlinks ---
\usepackage[
  colorlinks = true,
  linkcolor  = linkblue,
  citecolor  = linkblue,
  urlcolor   = linkblue,
  pdftitle   = {Large Language Models in Finance},
  pdfauthor  = {Juan F. Imbet},
  bookmarksnumbered = true,
]{hyperref}

% --- Bibliography ---
\usepackage[
  backend      = biber,
  style        = authoryear,
  maxcitenames = 2,
  maxbibnames  = 10,
  giveninits   = true,
  dashed       = false,
]{biblatex}
\addbibresource{bibliography.bib}
% natbib compatibility
\newcommand{\citet}[1]{\textcite{#1}}
\newcommand{\citep}[1]{\parencite{#1}}
\newcommand{\citealt}[1]{\cite{#1}}

% --- Theorem Environments ---
% Shared counter; reset per chapter
\newtheoremstyle{thmstyle}%
  {8pt}{6pt}%                     space above/below
  {\itshape}{}%                   body font; indent
  {\bfseries}{.}%                 head font; punctuation
  { }%                            space after head
  {\thmname{#1}\thmnumber{ #2}\thmnote{ \normalfont(#3)}}

\newtheoremstyle{defstyle}%
  {8pt}{6pt}%
  {\normalfont}{}%
  {\bfseries}{.}%
  { }%
  {\thmname{#1}\thmnumber{ #2}\thmnote{ \normalfont(#3)}}

\newtheoremstyle{remstyle}%
  {6pt}{4pt}%
  {\normalfont}{}%
  {\itshape}{.}%
  { }%
  {\thmname{#1}\thmnumber{ #2}\thmnote{ \normalfont(#3)}}

\theoremstyle{thmstyle}
\newtheorem{theorem}{Theorem}[chapter]
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{corollary}[theorem]{Corollary}

\theoremstyle{defstyle}
\newtheorem{definition}[theorem]{Definition}
\newtheorem{example}[theorem]{Example}
\newtheorem{illustration}[theorem]{Illustration}

\theoremstyle{remstyle}
\newtheorem{remark}[theorem]{Remark}

% --- PDF page inclusion (covers) ---
\usepackage{pdfpages}

% --- Miscellaneous ---
\usepackage{enumitem}
\setlist[enumerate]{itemsep=2pt, topsep=4pt}
\setlist[itemize]{itemsep=2pt, topsep=4pt}

% --- Dual-mode section environments ---
\usepackage[skins, breakable]{tcolorbox}

\newtcolorbox{deepdive}{%
  enhanced, breakable,
  colback  = deepdivebg,
  colframe = linkblue,
  boxrule  = 0pt,
  toprule  = 1.5pt,
  bottomrule = 0.5pt,
  leftrule = 0pt,
  rightrule = 0pt,
  arc      = 0pt,
  before upper = {%
    \noindent\hfill
    {\footnotesize\scshape\color{linkblue}under the hood}%
    \par\smallskip\noindent},
  left = 10pt, right = 10pt, top = 6pt, bottom = 8pt,
}

\newtcolorbox{context}{%
  enhanced, breakable,
  colback  = narrativebg,
  colframe = narrativerule,
  boxrule  = 0pt,
  toprule  = 1.5pt,
  bottomrule = 0.5pt,
  leftrule = 0pt,
  rightrule = 0pt,
  arc      = 0pt,
  before upper = {%
    \setlength{\parskip}{5pt}%
    \noindent\hfill
    {\footnotesize\scshape\color{narrativerule}the bigger picture}%
    \par\smallskip\noindent},
  after upper = {\setlength{\parskip}{3pt}},
  left = 10pt, right = 10pt, top = 6pt, bottom = 8pt,
}

% Tighter paragraph spacing (book style)
\setlength{\parskip}{3pt}
\setlength{\parindent}{1.5em}

% --- Title Block ---
\title{%
  \vspace{-1cm}
  {\large\scshape Large Language Models in Finance}\\[10pt]
  {\rule{\textwidth}{0.5pt}}\\[8pt]
  {\normalsize Juan F.\ Imbet}
}
\author{}
\date{}
