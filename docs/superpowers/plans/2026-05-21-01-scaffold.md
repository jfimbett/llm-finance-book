# Book-Course Template — Plan 1: Scaffold & Config

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Initialize the repository with the complete folder structure, base LaTeX/course/code placeholder files, master AI instructions (CLAUDE.md), Claude Code settings, and the TOPIC.md configuration schema.

**Architecture:** A monorepo with four top-level domains (`book/`, `course/`, `code/`, `.claude/`). All AI configuration lives under `.claude/`. One numbered unit (01-intro) is pre-scaffolded as a concrete example for users to follow. Everything else is empty directories with `.gitkeep` files.

**Tech Stack:** Git, pdflatex + BibTeX/Biber, Python 3, Jupyter, bash

---

## File Map

| File | Purpose |
|------|---------|
| `.gitignore` | Ignore LaTeX build artifacts, Python cache, Jupyter checkpoints |
| `README.md` | Quick-start instructions for template users |
| `TOPIC.md` | Project config schema — written by `/interview-me`, read by all hooks |
| `book/main.tex` | LaTeX master file — includes all chapters |
| `book/preamble.tex` | Packages, macros, theorem environments — filled in by `/interview-me` |
| `book/bibliography.bib` | Shared BibTeX file — starter entries |
| `book/chapters/01-intro/chapter.tex` | Example chapter placeholder |
| `course/lectures/01-intro/notes.md` | Example lecture notes placeholder |
| `course/lectures/01-intro/slides.tex` | Example Beamer slides placeholder |
| `course/lectures/01-intro/exercises.md` | Example exercises placeholder |
| `course/lectures/01-intro/solutions.md` | Example solutions placeholder |
| `code/src/__init__.py` | Shared Python package root |
| `code/notebooks/01-intro/demo.ipynb` | Minimal example notebook |
| `code/requirements.txt` | Python dependencies |
| `code/tests/__init__.py` | Test package root |
| `.claude/CLAUDE.md` | Master AI instructions loaded every session |
| `.claude/settings.json` | Hook registrations and permissions |
| `docs/STATUS.md` | Auto-generated status table (initial empty version) |
| `docs/SESSION_LOG.md` | Session log (initial empty version) |
| `scripts/validate-scaffold.sh` | Validation script — checks all required paths exist |

---

## Task 1: Initialize Git and Core Structure

**Files:**
- Create: `.gitignore`
- Create: `README.md`
- Create: `TOPIC.md`

- [ ] **Step 1: Initialize git repo**

```bash
cd "C:\Users\jfimb\Documents\book-course-template"
git init
```

Expected: `Initialized empty Git repository in .../book-course-template/.git/`

- [ ] **Step 2: Create directory tree**

```bash
mkdir -p book/chapters/01-intro/figures
mkdir -p book/style
mkdir -p course/lectures/01-intro
mkdir -p code/src
mkdir -p code/notebooks/01-intro
mkdir -p code/tests
mkdir -p .claude/agents
mkdir -p .claude/skills
mkdir -p .claude/hooks
mkdir -p docs/quality
mkdir -p docs/superpowers/specs
mkdir -p docs/superpowers/plans
mkdir -p scripts
mkdir -p hooks
```

- [ ] **Step 3: Create .gitignore**

Create `.gitignore` with this exact content:

```
# LaTeX build artifacts
*.aux
*.log
*.out
*.toc
*.lof
*.lot
*.fls
*.fdb_latexmk
*.synctex.gz
*.bbl
*.bcf
*.blg
*.run.xml
book/*.pdf
book/**/*.pdf

# Python
__pycache__/
*.pyc
*.pyo
.env
.venv/
env/
venv/
*.egg-info/
dist/
build/
.pytest_cache/

# Jupyter
.ipynb_checkpoints/
*/.ipynb_checkpoints/

# Editor
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Solutions (optional — remove if publishing solutions)
course/lectures/*/solutions.md

# Quality reports are tracked
!docs/quality/
```

- [ ] **Step 4: Create README.md**

```markdown
# Book-Course Template

An AI-assisted template for creating a structured course, companion LaTeX book, and runnable code.

## Quick Start

1. Clone this repository
2. Open it in Claude Code (or any AI assistant)
3. Run `/interview-me` — this configures the entire project in one interactive session
4. Run `/new-topic` — scaffold your first chapter
5. Run `/draft-chapter` — start writing

## Structure

```
book/          LaTeX book (deeper treatment)
course/        Lecture notes, slides, exercises
code/          Jupyter notebooks + shared Python package
.claude/       AI agents, skills, and hooks
docs/          Design specs, quality reports, status
```

## Requirements

- pdflatex + biber (TeX Live or MiKTeX)
- Python 3.9+
- Jupyter

## Documentation

See `docs/superpowers/specs/` for the full design specification.
```

- [ ] **Step 5: Create TOPIC.md**

```markdown
---
title: ""
author: ""
institution: ""
email: ""
license: ""
audience: ""
language: "python"
chapters_planned: 0
book_depth: "loose"
quality_threshold: 7
max_refine_iterations: 5
auto_commit: true
git_remote: ""
---

# Topic Configuration

This file is generated and maintained by the `/interview-me` skill.
Do not edit manually — run `/interview-me` to update any settings.

## What This Course/Book Is About

[Generated by /interview-me]

## Audience

[Generated by /interview-me]

## Quality Gates

- Minimum score per dimension: `quality_threshold` (0–10)
- Max auto-refinement iterations: `max_refine_iterations`
- Auto-commit on save: `auto_commit`
```

- [ ] **Step 6: Commit**

```bash
git add .gitignore README.md TOPIC.md
git commit -m "chore: initialize repo with core config files"
```

---

## Task 2: LaTeX Book Scaffold

**Files:**
- Create: `book/main.tex`
- Create: `book/preamble.tex`
- Create: `book/bibliography.bib`
- Create: `book/chapters/01-intro/chapter.tex`

- [ ] **Step 1: Create book/main.tex**

```latex
\documentclass[12pt, oneside]{book}

\input{preamble}

\begin{document}

\frontmatter
\maketitle
\tableofcontents
\listoffigures

\mainmatter
\include{chapters/01-intro/chapter}
% Add new chapters here: \include{chapters/02-name/chapter}

\backmatter
\bibliographystyle{plain}
\bibliography{bibliography}

\end{document}
```

- [ ] **Step 2: Create book/preamble.tex**

```latex
% ============================================================
% Preamble — packages, macros, theorem environments
% Author and title fields are filled in by /interview-me
% ============================================================

% --- Core packages ---
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage[english]{babel}

% --- Math ---
\usepackage{amsmath, amssymb, amsthm, mathtools}

% --- Figures & Tables ---
\usepackage{graphicx}
\usepackage{tikz}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usepackage{booktabs}
\usepackage{float}

% --- Code listings ---
\usepackage{listings}
\lstset{basicstyle=\ttfamily\small, breaklines=true, frame=single}

% --- Hyperlinks ---
\usepackage[colorlinks=true, linkcolor=blue, citecolor=blue, urlcolor=blue]{hyperref}

% --- Bibliography ---
\usepackage[backend=biber, style=authoryear]{biblatex}
\addbibresource{bibliography.bib}

% --- Theorem environments ---
\theoremstyle{plain}
\newtheorem{theorem}{Theorem}[chapter]
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{corollary}[theorem]{Corollary}

\theoremstyle{definition}
\newtheorem{definition}[theorem]{Definition}
\newtheorem{example}[theorem]{Example}
\newtheorem{exercise}[theorem]{Exercise}

\theoremstyle{remark}
\newtheorem{remark}[theorem]{Remark}

% --- Title block (filled by /interview-me) ---
\title{COURSE TITLE}
\author{AUTHOR NAME \\ INSTITUTION}
\date{\today}
```

- [ ] **Step 3: Create book/bibliography.bib**

```bibtex
% Bibliography — add entries here or let the literature-reviewer agent suggest them.
% Format: BibTeX, compiled with biber.

@book{example2024,
  author    = {Last, First},
  title     = {Example Reference},
  publisher = {Publisher},
  year      = {2024},
  address   = {City},
}
```

- [ ] **Step 4: Create book/chapters/01-intro/chapter.tex**

```latex
\chapter{Introduction}
\label{ch:intro}

% TODO: Replace this placeholder with actual content.
% Invoke /draft-chapter to generate a first draft.

\section{Motivation}
\label{sec:motivation}

[Motivation placeholder]

\section{Overview}
\label{sec:overview}

[Overview placeholder]

\section{Prerequisites}
\label{sec:prereqs}

[Prerequisites placeholder]
```

- [ ] **Step 5: Add .gitkeep to empty directories**

```bash
touch book/style/.gitkeep
touch book/chapters/01-intro/figures/.gitkeep
```

- [ ] **Step 6: Commit**

```bash
git add book/
git commit -m "chore: scaffold LaTeX book structure with intro chapter placeholder"
```

---

## Task 3: Course Scaffold

**Files:**
- Create: `course/lectures/01-intro/notes.md`
- Create: `course/lectures/01-intro/slides.tex`
- Create: `course/lectures/01-intro/exercises.md`
- Create: `course/lectures/01-intro/solutions.md`

- [ ] **Step 1: Create course/lectures/01-intro/notes.md**

```markdown
# Lecture 1: Introduction

**Paired chapter:** `book/chapters/01-intro/chapter.tex`
**Learning objectives:**
- [ ] Objective 1
- [ ] Objective 2
- [ ] Objective 3

---

## 1. Motivation

[Lecture notes placeholder — invoke /draft-lecture to generate]

## 2. Core Concepts

[Placeholder]

## 3. Summary

[Placeholder]

## Further Reading

See Chapter 1 of the companion book for a deeper treatment, including proofs and additional examples.
```

- [ ] **Step 2: Create course/lectures/01-intro/slides.tex**

```latex
\documentclass{beamer}
\usetheme{Madrid}
\usepackage{amsmath, amssymb}

\title{Lecture 1: Introduction}
\author{AUTHOR NAME}
\institute{INSTITUTION}
\date{\today}

\begin{document}

\begin{frame}
  \titlepage
\end{frame}

\begin{frame}{Outline}
  \tableofcontents
\end{frame}

\section{Motivation}
\begin{frame}{Motivation}
  % TODO: invoke /draft-lecture to fill in slides
  \begin{itemize}
    \item Point 1
    \item Point 2
    \item Point 3
  \end{itemize}
\end{frame}

\section{Core Concepts}
\begin{frame}{Core Concepts}
  \begin{itemize}
    \item Concept 1
    \item Concept 2
  \end{itemize}
\end{frame}

\begin{frame}{Summary}
  \begin{itemize}
    \item Key takeaway 1
    \item Key takeaway 2
  \end{itemize}
\end{frame}

\end{document}
```

- [ ] **Step 3: Create course/lectures/01-intro/exercises.md**

```markdown
# Exercises — Lecture 1: Introduction

**Difficulty tags:** [B] = Beginner, [I] = Intermediate, [A] = Advanced

---

**Exercise 1.1 [B]**

[Exercise placeholder — invoke /draft-exercises to generate]

---

**Exercise 1.2 [I]**

[Placeholder]

---

**Exercise 1.3 [A]**

[Placeholder]
```

- [ ] **Step 4: Create course/lectures/01-intro/solutions.md**

```markdown
# Solutions — Lecture 1: Introduction

> **Note:** This file may be gitignored in student-facing repositories.
> See `.gitignore` to control visibility.

---

**Solution 1.1**

[Solution placeholder]

---

**Solution 1.2**

[Placeholder]

---

**Solution 1.3**

[Placeholder]
```

- [ ] **Step 5: Commit**

```bash
git add course/
git commit -m "chore: scaffold course lecture 01-intro with placeholder files"
```

---

## Task 4: Code Scaffold

**Files:**
- Create: `code/src/__init__.py`
- Create: `code/notebooks/01-intro/demo.ipynb`
- Create: `code/requirements.txt`
- Create: `code/tests/__init__.py`

- [ ] **Step 1: Create code/src/__init__.py**

```python
"""
Shared package for {COURSE TITLE} companion code.
Notebooks in code/notebooks/ import from this package.
"""
```

- [ ] **Step 2: Create code/requirements.txt**

```
# Core
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
scipy>=1.11

# Notebooks
jupyter>=1.0
ipykernel>=6.0

# Testing
pytest>=7.0
pytest-cov>=4.0

# Code quality
black>=23.0
```

- [ ] **Step 3: Create code/tests/__init__.py**

```python
# Test package for companion code
```

- [ ] **Step 4: Create code/notebooks/01-intro/demo.ipynb**

```json
{
 "cells": [
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": ["# Demo — Lecture 1: Introduction\n", "\n", "This notebook accompanies Lecture 1 and Chapter 1.\n", "Invoke `/draft-code` to generate the full demo."]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": ["# Imports\nimport numpy as np\nimport matplotlib.pyplot as plt\n\n# TODO: invoke /draft-code to fill in this notebook"]
  }
 ],
 "metadata": {
  "kernelspec": {
   "display_name": "Python 3",
   "language": "python",
   "name": "python3"
  },
  "language_info": {
   "name": "python",
   "version": "3.9.0"
  }
 },
 "nbformat": 4,
 "nbformat_minor": 5
}
```

- [ ] **Step 5: Commit**

```bash
git add code/
git commit -m "chore: scaffold Python code structure with starter notebook"
```

---

## Task 5: Master AI Instructions (CLAUDE.md)

**Files:**
- Create: `.claude/CLAUDE.md`

- [ ] **Step 1: Create .claude/CLAUDE.md**

```markdown
# CLAUDE.md — Master AI Instructions

This file is loaded at the start of every AI session. It tells the AI assistant
what this project is, how it is structured, and how to behave.

---

## What This Project Is

This is a book-course template. It contains:
- A LaTeX book (`book/`) — deep treatment with proofs and references
- A course (`course/`) — lectures, slides, and exercises (a subset of the book)
- Companion code (`code/`) — Jupyter notebooks + a shared Python package
- AI agents (`.claude/agents/`) — role-based personas for specific tasks
- AI skills (`.claude/skills/`) — workflow scripts that orchestrate agents

The subject matter, audience, and quality settings are defined in `TOPIC.md`.
**Always read `TOPIC.md` at the start of any content task.**

---

## Project Conventions

1. **Numbering:** Chapter N and Lecture N cover the same topic. The book goes deeper.
   `book/chapters/01-intro/` ↔ `course/lectures/01-intro/` ↔ `code/notebooks/01-intro/`

2. **Quality gate:** Before committing any `.tex` or `.md` content file, the quality
   score for each dimension (clarity, rigor, completeness, pedagogy, style) must be
   ≥ `quality_threshold` from `TOPIC.md`. Run `/score-content` to check.

3. **Auto-commit:** If `auto_commit: true` in `TOPIC.md`, stage and commit after
   every file write with a structured message: `feat(ch01): [summary]`

4. **Agents are single-purpose:** When asked to do a task, invoke the right agent.
   Do not merge roles. The scorer scores; the editor edits; the proofreader proofs.

5. **Skills are workflows:** Run a skill when the user invokes it by name (e.g.,
   `/draft-chapter`). Follow the skill's steps exactly.

---

## Directory Quick Reference

```
.claude/agents/     Role-based AI personas — read the relevant one before acting
.claude/skills/     Workflow scripts — follow step by step
.claude/hooks/      Shell hooks — do not modify unless updating hook behavior
book/               LaTeX source — compile with pdflatex + biber
course/             Lecture materials — markdown + Beamer
code/               Python package + Jupyter notebooks
docs/quality/       JSON score reports — read these to see what needs improvement
docs/STATUS.md      Auto-generated chapter status table
TOPIC.md            Project configuration — read this first on every content task
```

---

## How to Use Agents

Agents are in `.claude/agents/`. Each is a markdown file describing a role.
To use an agent: read its file, adopt its persona, then perform its task.

Example: to review math in a chapter —
1. Read `.claude/agents/math-checker.md`
2. Adopt the math-checker persona
3. Review the target file as instructed

---

## How to Use Skills

Skills are in `.claude/skills/`. Each describes a multi-step workflow.
To run a skill: read its file, follow its steps in order.

Common entry points:
- `/interview-me` — configure this project (run first)
- `/new-topic` — scaffold a new chapter/lecture/notebook unit
- `/draft-chapter` — write a chapter
- `/draft-lecture` — write a lecture from a chapter
- `/score-content [file]` — score a file and write a quality report
- `/refine-until-threshold [file]` — iteratively improve until quality passes
- `/topic-status` — print the status of all chapters

---

## Commit Message Format

```
<type>(<scope>): <summary>

Types: feat | fix | refine | chore | docs
Scope: ch01 | lec01 | code01 | agents | skills | hooks | config
```

Examples:
- `feat(ch01): draft introduction section`
- `refine(ch01): improve clarity score from 6 to 8`
- `chore: scaffold chapter 02`
```

- [ ] **Step 2: Commit**

```bash
git add .claude/CLAUDE.md
git commit -m "chore: add master AI instructions"
```

---

## Task 6: Claude Code Settings

**Files:**
- Create: `.claude/settings.json`

- [ ] **Step 1: Create .claude/settings.json**

```json
{
  "hooks": {
    "PreToolUse": [],
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "bash .claude/hooks/auto-commit.sh \"$CLAUDE_TOOL_INPUT_PATH\"",
            "description": "Auto-commit file writes when auto_commit=true in TOPIC.md"
          },
          {
            "type": "command",
            "command": "bash .claude/hooks/score-on-save.sh \"$CLAUDE_TOOL_INPUT_PATH\"",
            "description": "Score content files on save and write quality report"
          },
          {
            "type": "command",
            "command": "bash .claude/hooks/scaffold-pairs.sh \"$CLAUDE_TOOL_INPUT_PATH\"",
            "description": "Auto-create paired lecture/notebook when a new chapter is created"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash .claude/hooks/session-log.sh",
            "description": "Append session summary to docs/SESSION_LOG.md"
          }
        ]
      }
    ],
    "PreCommit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash .claude/hooks/check-latex.sh",
            "description": "Verify LaTeX compiles before commit"
          },
          {
            "type": "command",
            "command": "bash .claude/hooks/check-refs.sh",
            "description": "Check for undefined LaTeX references"
          },
          {
            "type": "command",
            "command": "bash .claude/hooks/check-bib.sh",
            "description": "Validate bibliography entries"
          },
          {
            "type": "command",
            "command": "bash .claude/hooks/check-notation.sh",
            "description": "Check notation consistency"
          },
          {
            "type": "command",
            "command": "bash .claude/hooks/check-numbering.sh",
            "description": "Verify chapter/lecture pairs exist"
          },
          {
            "type": "command",
            "command": "bash .claude/hooks/gate-check.sh",
            "description": "Block commit if quality scores below threshold"
          }
        ]
      }
    ]
  },
  "permissions": {
    "allow": [
      "Bash(git *)",
      "Bash(pdflatex *)",
      "Bash(biber *)",
      "Bash(bash scripts/*)",
      "Bash(bash .claude/hooks/*)",
      "Bash(python *)",
      "Bash(pytest *)",
      "Bash(jupyter *)"
    ]
  }
}
```

- [ ] **Step 2: Commit**

```bash
git add .claude/settings.json
git commit -m "chore: register hooks and permissions in Claude Code settings"
```

---

## Task 7: Docs Scaffold and Validation Script

**Files:**
- Create: `docs/STATUS.md`
- Create: `docs/SESSION_LOG.md`
- Create: `scripts/validate-scaffold.sh`

- [ ] **Step 1: Create docs/STATUS.md**

```markdown
# Chapter Status

> Auto-generated by `update-status.sh`. Do not edit manually.

| # | Chapter / Lecture | Draft | Scored | Clarity | Rigor | Completeness | Pedagogy | Style | Released |
|---|-------------------|-------|--------|---------|-------|--------------|----------|-------|----------|
| 01 | Introduction | No | No | — | — | — | — | — | No |
```

- [ ] **Step 2: Create docs/SESSION_LOG.md**

```markdown
# Session Log

> Auto-appended by `session-log.sh` at the end of each AI session.

| Date | Files Changed | Score Deltas | Open Items |
|------|---------------|--------------|------------|
```

- [ ] **Step 3: Create scripts/validate-scaffold.sh**

```bash
#!/bin/bash
# validate-scaffold.sh — verifies the scaffold is complete
# Run: bash scripts/validate-scaffold.sh

set -e
PASS=0
FAIL=0

check() {
  if [ -e "$1" ]; then
    echo "  OK  $1"
    PASS=$((PASS+1))
  else
    echo "MISS  $1"
    FAIL=$((FAIL+1))
  fi
}

echo "=== Scaffold Validation ==="

# Core config
check "TOPIC.md"
check "README.md"
check ".gitignore"

# Claude config
check ".claude/CLAUDE.md"
check ".claude/settings.json"
check ".claude/agents"
check ".claude/skills"
check ".claude/hooks"

# Book
check "book/main.tex"
check "book/preamble.tex"
check "book/bibliography.bib"
check "book/chapters/01-intro/chapter.tex"

# Course
check "course/lectures/01-intro/notes.md"
check "course/lectures/01-intro/slides.tex"
check "course/lectures/01-intro/exercises.md"
check "course/lectures/01-intro/solutions.md"

# Code
check "code/src/__init__.py"
check "code/notebooks/01-intro/demo.ipynb"
check "code/requirements.txt"
check "code/tests/__init__.py"

# Docs
check "docs/STATUS.md"
check "docs/SESSION_LOG.md"
check "docs/quality"

echo ""
echo "Result: $PASS passed, $FAIL missing"
[ "$FAIL" -eq 0 ] && echo "Scaffold complete." || exit 1
```

- [ ] **Step 4: Run validation**

```bash
bash scripts/validate-scaffold.sh
```

Expected output: all items show `OK`, final line reads `Scaffold complete.`

- [ ] **Step 5: Final commit**

```bash
git add docs/ scripts/validate-scaffold.sh
git commit -m "chore: add status/log docs and scaffold validation script"
```

---

## Self-Review

- All required directories created ✓
- TOPIC.md has the full YAML schema from the spec ✓  
- CLAUDE.md covers all conventions from the spec (numbering, quality gate, auto-commit, agent/skill usage) ✓
- settings.json registers all 13 hooks from the spec across PreCommit / PostToolUse / Stop ✓
- Validation script covers every required path ✓
- No TBD or placeholder steps — every step has exact commands or file content ✓
